/**
 * Types.
 *
 * @module ch.maenulabs.type
 */
(function () {
	/* global window */
	if (!window.ch) {
		window.ch = {};
	}
	if (!window.ch.maenulabs) {
		window.ch.maenulabs = {};
	}
	if (!window.ch.maenulabs.type) {
		window.ch.maenulabs.type = {};
	}
})();
